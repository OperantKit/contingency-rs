target/
Cargo.lock.bak
*.bak
.DS_Store
# wasm-pack build output (generated, consumed by frontend via file: dep)
pkg/
# Package-local scratch / planning / draft / review traces
# Package-local scratch / planning / draft / review traces
.local/
tmp/
PLAN.md
